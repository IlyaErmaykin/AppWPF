<Window x:Class="AppWPF.ChildWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppWPF"
        mc:Ignorable="d"
        Title="ChildWindow" Height="300" Width="300">

    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close"
                        Executed="Executed_CloseWindow"
                        CanExecute="CanExecuted_CloseWindow" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="ApplicationCommands.Close"
                    Gesture="Esc" />
    </Window.InputBindings>
    <!--<Window.InputBinding>
        <KeyBinding Command="Exit" Key="Esc" Modifiers="Control"/>
    </Window.InputBinding>-->
    <Grid>
        <StackPanel>
            <Label HorizontalAlignment="Center" Content="      Для закрытия окна &#10; горячая клавиша [Ctrl+N]" />
            <Button x:Name="ExitButtonWindow" Width="100" Height="30" Content="Exit" Click="ExitWindowButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
